import React from "react";
import styled from "styled-components";

const TodoHeaderStyle = styled.header`
flex-grow: 1;
`;

const TodoHeader = () => {
    return (
        <TodoHeaderStyle>

        </TodoHeaderStyle>
    );
};

export default TodoHeader;